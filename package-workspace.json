{
  "name": "lyrix-studio-workspace",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix .",
    "dev:frontend": "npm run dev --prefix .",
    "dev:backend": "npm run dev --prefix backend",
    "build:frontend": "npm run build --prefix .",
    "build:backend": "npm run build --prefix backend",
    "setup:backend": "cd backend && npx prisma generate && npx prisma db push && npm run db:seed"
  }
}
